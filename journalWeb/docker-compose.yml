version: '3.8'

services:
  journal-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: journal-web
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
